(jbuild_version 1)

(executable
 ((name minotaure)
  (public_name minotaure)
  (libraries (async
    core
    ocaml_plugin_minotaure
    sexplib
    cmdliner notty notty.unix unix logs fmt logs.fmt ocaml_plugin))
  (modules (minotaure))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
  )
)
